name: "Shellcheck"

on:
  push:
    branch:
      - main
  pull_request:

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.0.1
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
